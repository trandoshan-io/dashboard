// If you want to use Trandoshan in production you'll have to add your corresponding apiUrl here
// And build the application using ng build --prod (app build) or by using docker build --build_arg build_mode=--prod (docker build)

export const environment = {
  production: true
};
